#version 460
#extension GL_NV_mesh_shader : require
 
layout(local_size_x = 1) in;

uniform float ilevel;
uniform float olevel;

void main()
{
    //int i_u_lvls = int(ceil(ilevel));
    //i_u_lvls -= i_u_lvls % 2;

    //int o_u_lvls = int(ceil(olevel));
    //o_u_lvls += 1 - o_u_lvls % 2;

    //int v_lvls = int(ceil(ilevel));
    //v_lvls += 1 - v_lvls % 2;
    //v_lvls = int(ceil(v_lvls * 0.5));

    //gl_TaskCountNV = 3 * v_lvls;
    gl_TaskCountNV = 3;
}
